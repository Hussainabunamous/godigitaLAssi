class ItemNews {
  int? id;
  int? index;
  String? imgUrl;
  String? itenPrice;
  String? itemTitle;
  String? itemDescription;

  ItemNews(this.id, this.index, this.imgUrl, this.itenPrice, this.itemTitle,
      this.itemDescription);
}
